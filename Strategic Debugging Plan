# 🎯 Strategic Debugging Plan — traopd1 VALORANT Tracker

## 1. System Decomposition Overview

- **Primary Goal:** Identify and fix all bugs, improve code quality, harden security, and optimize performance across the `traopd1` VALORANT Tracker — an Electron + React + Express app that reads Riot's local API to show live match data, player skins, rank info, and match history.
- **Complexity Assessment:** **Medium-High** — The codebase spans 4 layers (Electron shell → React frontend → Express API server → Riot local API integration) with a critical logic bug, security vulnerabilities, performance bottlenecks, a 994-line dead legacy file, and zero existing tests.

### Architecture Map

```mermaid
graph TD
    A["Electron Shell<br/>electron/main.js"] --> B["React Frontend<br/>src/App.tsx + 10 Components"]
    A --> C["Express API Server<br/>server/valorant.js (738 lines)"]
    C --> D["Riot Local API<br/>Lockfile + Presences + GLZ/PD"]
    C --> E["valorant-api.com<br/>Agent Icons / Maps / Skins"]
    F["app.js (994 lines)<br/>DEAD LEGACY CODE"] -.->|"Not loaded anywhere"| A
```

---

## 2. The Quad-Agent Operational Plans

---

### 🛡️ Plan 1 — Agent A: Syntax & Standards Guard

**Objective:** Fix syntax errors, type issues, and naming inconsistencies.

#### Findings

| #   | File                                                                                                         | Line  | Issue                                                                                                                                                                                                       | Severity    |
| --- | ------------------------------------------------------------------------------------------------------------ | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| A1  | [App.tsx](file:///c:/Users/Administrator/Desktop/traopd1/src/App.tsx#L34)                                    | 34    | **Critical logic bug:** `!error === false` is always `false` (negates before comparing). Should be `error !== null` or `!error`                                                                             | 🔴 Critical |
| A2  | [App.tsx](file:///c:/Users/Administrator/Desktop/traopd1/src/App.tsx#L34)                                    | 34    | Condition `gameState === 'NOT_RUNNING' \|\| !error === false` should likely be `gameState === 'NOT_RUNNING' \|\| error` — the current form means "NOT_RUNNING" screen **never** shows when there's an error | 🔴 Critical |
| A3  | [app.js](file:///c:/Users/Administrator/Desktop/traopd1/app.js)                                              | 1–994 | Entire 994-line file is **dead code** — not imported or referenced by `index.html`, `main.tsx`, or any module. It's a legacy vanilla JS implementation superseded by the React app                          | 🟡 Medium   |
| A4  | [PlayerCard.tsx](file:///c:/Users/Administrator/Desktop/traopd1/src/components/PlayerCard.tsx#L117)          | 117   | Uses array `index` as React `key` instead of a stable identifier for history entries                                                                                                                        | 🟢 Low      |
| A5  | [main.tsx](file:///c:/Users/Administrator/Desktop/traopd1/src/main.tsx#L7)                                   | 7     | Missing `<React.StrictMode>` wrapper—not a bug but a best-practice omission                                                                                                                                 | 🟢 Low      |
| A6  | [PlayerProfile.tsx](file:///c:/Users/Administrator/Desktop/traopd1/src/components/PlayerProfile.tsx#L22-L30) | 22–30 | `WEAPON_SLOTS` constant is declared but **never used** (dead code within the component)                                                                                                                     | 🟢 Low      |

#### Recommended Fixes

```diff
// App.tsx line 34 — CRITICAL FIX
- if (gameState === 'NOT_RUNNING' || !error === false) {
+ if (gameState === 'NOT_RUNNING' || error) {
```

```diff
// PlayerProfile.tsx — Remove dead constant
- const WEAPON_SLOTS = [
-   { key: 'sidearms', label: 'Sidearms', names: ['Classic','Shorty','Frenzy','Ghost','Sheriff'] },
-   ...
- ];
```

---

### 🧠 Plan 2 — Agent B: Logic & Flow Architect

**Objective:** Analyze business logic, edge cases, state management, and algorithmic correctness.

#### Findings

| #   | File                                                                                               | Line(s) | Issue                                                                                                                                                                                                                                                        | Severity           |
| --- | -------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ |
| B1  | [App.tsx](file:///c:/Users/Administrator/Desktop/traopd1/src/App.tsx#L34)                          | 34      | The `!error === false` condition creates a **dead branch** — the "VALORANT Not Running" UI can never render correctly when the game is actually not running _and_ there's a fetch error. Users see a blank screen instead                                    | 🔴 Critical        |
| B2  | [useValorant.ts](file:///c:/Users/Administrator/Desktop/traopd1/src/hooks/useValorant.ts#L65-L88)  | 65–88   | **Race condition:** `poll()` can trigger `fetchMatch()` while a previous call is in-flight _and_ a `setState` from line 72 hasn't settled yet. The `isFetchingMatch` ref helps but `setState` from line 72 can intermediate-overwrite the match from line 85 | 🟡 Medium          |
| B3  | [useValorant.ts](file:///c:/Users/Administrator/Desktop/traopd1/src/hooks/useValorant.ts#L98-L108) | 98–108  | History refetch triggers on **every** MENUS state visit (including repeated polls), not just on transition from INGAME→MENUS. Could cause excessive API calls                                                                                                | 🟡 Medium          |
| B4  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L364)              | 364     | `RankedRatingEarned >= 0` classifies a draw (RR=0) as a "win". Edge case: a loss with exactly 0 RR earned would be misclassified                                                                                                                             | 🟡 Medium          |
| B5  | [renderModalContent](file:///c:/Users/Administrator/Desktop/traopd1/app.js#L322-L326)              | 322–326 | `renderModalContent()` handles only `loadout` and `stats` tabs but tab switching code (line 573) sets `currentTab` to any string from `data-tab` (including `history`). The history tab content never renders                                                | 🟢 Low (dead code) |
| B6  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L60-L68)           | 60–68   | `getLockfile()` doesn't handle file locking — if the Riot Client is mid-write, partial read can yield corrupted data                                                                                                                                         | 🟢 Low             |

#### Recommended Fixes

**B1 — Fix state logic in `App.tsx`:**

```tsx
// The condition should be:
if (gameState === "NOT_RUNNING" || error) {
  // Show "not running" UI
}
```

**B3 — Debounce history fetch on state transition only:**

```ts
// Track previous gameState and only refetch when transitioning from INGAME→MENUS
const wasInMatch = useRef(false);
useEffect(() => {
  if (state.gameState === "INGAME" || state.gameState === "PREGAME") {
    wasInMatch.current = true;
  }
  if (state.gameState === "MENUS" && wasInMatch.current) {
    wasInMatch.current = false;
    const timer = setTimeout(() => {
      /* refetch */
    }, 5000);
    return () => clearTimeout(timer);
  }
}, [state.gameState]);
```

---

### 🔒 Plan 3 — Agent C: Performance & Security Auditor

**Objective:** Identify memory leaks, bottlenecks, and security vulnerabilities.

#### Security Findings

| #   | File                                                                                        | Line(s) | Issue                                                                                                                                                                             | Severity    |
| --- | ------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| C1  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L12)        | 12      | **Wildcard CORS** (`Access-Control-Allow-Origin: *`) — allows any website to make requests to the local API server, potentially leaking match data and auth tokens                | 🔴 Critical |
| C2  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L17)        | 17      | `rejectUnauthorized: false` disables SSL cert verification globally. Required for Riot's self-signed local certs but should be scoped only to `127.0.0.1` requests, not all HTTPS | 🟡 Medium   |
| C3  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L636-L637)  | 636–637 | Raw `e.message` error leaks in 500 responses — could expose internal paths, stack traces, or Riot API details to the client                                                       | 🟡 Medium   |
| C4  | [electron/main.js](file:///c:/Users/Administrator/Desktop/traopd1/electron/main.js#L34-L36) | 34–36   | `nodeIntegration: false` + `contextIsolation: true` is good, but `preload.js` exposes `package.json` version via `contextBridge` — minor info leak                                | 🟢 Low      |

#### Performance Findings

| #   | File                                                                                       | Line(s) | Issue                                                                                                                                                                                               | Severity    |
| --- | ------------------------------------------------------------------------------------------ | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| C5  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L553-L563) | 553–563 | **Unbounded parallel API calls** — fires N rank requests + N history requests simultaneously (up to 10 players × 2 = 20 concurrent requests). Can hit Riot rate limits and cause cascading timeouts | 🔴 Critical |
| C6  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L660-L665) | 660–665 | `Promise.allSettled()` on up to 10 match detail fetches runs in parallel with no concurrency limit                                                                                                  | 🟡 Medium   |
| C7  | [valorant.js](file:///c:/Users/Administrator/Desktop/traopd1/server/valorant.js#L263)      | 263     | Weapons API fetch runs **after** agents+maps, not in parallel — adds unnecessary sequential latency to `getContent()`                                                                               | 🟡 Medium   |
| C8  | [styles.css](file:///c:/Users/Administrator/Desktop/traopd1/styles.css)                    | —       | 62KB CSS file likely contains massive unused styles from the dead `app.js` code                                                                                                                     | 🟢 Low      |

#### Recommended Fixes

**C1 — Restrict CORS to localhost only:**

```js
app.use((req, res, next) => {
  const origin = req.headers.origin;
  if (
    origin &&
    (origin.startsWith("http://localhost") ||
      origin.startsWith("http://127.0.0.1"))
  ) {
    res.header("Access-Control-Allow-Origin", origin);
  }
  next();
});
```

**C5 — Batch API calls with concurrency limit:**

```js
// Use a simple chunked approach instead of firing all at once
async function batchedPromises(items, fn, concurrency = 3) {
  const results = [];
  for (let i = 0; i < items.length; i += concurrency) {
    const batch = items.slice(i, i + concurrency);
    results.push(...(await Promise.all(batch.map(fn))));
  }
  return results;
}
```

**C7 — Parallelize all content fetches:**

```diff
- const [agentsRes, mapsRes] = await Promise.all([...]);
- const weaponsRes = await axios.get(...); // sequential!
+ const [agentsRes, mapsRes, weaponsRes] = await Promise.all([
+   axios.get('https://valorant-api.com/v1/agents?isPlayableCharacter=true', { timeout: 10000 }),
+   axios.get('https://valorant-api.com/v1/maps', { timeout: 10000 }),
+   axios.get('https://valorant-api.com/v1/weapons', { timeout: 10000 }),
+ ]);
```

---

### 🔄 Plan 4 — Agent D: Integration & Regression Expert

**Objective:** Ensure fixes don't break existing features; plan test coverage.

#### Current Test Coverage: **0%** (No tests exist)

#### Integration Risk Assessment

| Fix                        | Risk                                                                                                                          | Mitigation                                             |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| A1: `!error === false` fix | **High** — Changes flow of the main UI state machine. Could show wrong screen if error/gameState interaction is misunderstood | Verify all 5 gameState transitions manually in browser |
| C1: CORS restriction       | **Medium** — Could break dev mode if Vite dev server origin doesn't match                                                     | Test `npm run dev` workflow end-to-end                 |
| C5: Batched API calls      | **Medium** — Changes timing of data returned to frontend; could cause UI flicker if partial data arrives                      | Test live match with full lobby                        |
| B3: History debounce       | **Low** — Only affects timing of history refetch                                                                              | Verify history page shows data after match ends        |

#### Test Plan

> [!IMPORTANT]
> There are **zero existing tests** in this project. No `jest`, `vitest`, or any test runner is configured.

**1. Build Verification** (automated):

```bash
cd c:\Users\Administrator\Desktop\traopd1
npx tsc --noEmit          # TypeScript type checking
npx vite build            # Ensure production build succeeds
```

**2. Manual Browser Testing** (must be done by user with VALORANT running):

1. Start the app: `npm run dev`
2. With VALORANT **closed** → verify the "VALORANT Not Running" screen appears (validates A1/B1 fix)
3. Open VALORANT, sit in menus → verify "Waiting for Match" screen appears
4. Queue for a game → verify live match data loads with player cards
5. Check player profile modal opens on click with rank/skins data
6. Navigate to History tab → verify match history loads
7. Check browser DevTools Network tab → verify no CORS errors on `localhost:3001` calls

---

## 3. Orchestration Workflow

### Handover Protocol

```mermaid
graph LR
    A["Agent A<br/>Syntax Guard"] -->|"Fixed code passes to"| B["Agent B<br/>Logic Architect"]
    B -->|"Logic-verified code passes to"| C["Agent C<br/>Security Auditor"]
    C -->|"Hardened code passes to"| D["Agent D<br/>Integration Expert"]
    D -->|"Final validated build"| E["✅ Deploy"]
```

1. **A → B:** After syntax fixes, Agent B validates the corrected `App.tsx:34` condition aligns with intended state machine behavior
2. **B → C:** After logic fixes, Agent C scans for any new security implications (e.g., error messages changed in state handling)
3. **C → D:** After security hardening, Agent D runs build verification and documents manual test steps
4. **D → Deploy:** Final `npx vite build` + `npx tsc --noEmit` must pass with zero errors

### Success Metrics

| KPI                     | Target       | Current                                        |
| ----------------------- | ------------ | ---------------------------------------------- |
| Critical bugs           | 0            | **3** (A1, C1, C5)                             |
| TypeScript type errors  | 0            | TBD — needs `tsc --noEmit` run                 |
| Build passes            | ✅           | TBD                                            |
| Dead code removed       | 100%         | 0% (994-line `app.js` + unused `WEAPON_SLOTS`) |
| CORS hardened           | ✅           | ❌ Wildcard `*`                                |
| Test coverage           | > 0%         | **0%**                                         |
| API concurrency limited | ≤ 3 parallel | Unbounded (~20)                                |

---

## Priority Execution Order

| Priority  | Issue                                     | Agent | Impact                                |
| --------- | ----------------------------------------- | ----- | ------------------------------------- |
| 🔴 **P0** | `!error === false` logic bug (A1/B1)      | A + B | UI completely broken for error states |
| 🔴 **P0** | Wildcard CORS (C1)                        | C     | Security vulnerability                |
| 🔴 **P0** | Unbounded parallel API calls (C5)         | C     | Rate-limiting & timeouts              |
| 🟡 **P1** | Race condition in `useValorant` (B2)      | B     | Intermittent stale data               |
| 🟡 **P1** | History refetch spam (B3)                 | B     | Excessive API calls                   |
| 🟡 **P1** | Error message leakage (C3)                | C     | Info disclosure                       |
| 🟡 **P1** | Sequential content fetch (C7)             | C     | Unnecessary latency                   |
| 🟢 **P2** | Dead code removal (A3, A6)                | A     | Code hygiene                          |
| 🟢 **P2** | Win/loss misclassification edge case (B4) | B     | Data accuracy                         |
